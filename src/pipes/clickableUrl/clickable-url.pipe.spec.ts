import { ClickableUrlPipe } from './clickable-url.pipe';

describe('ClickableUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ClickableUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
